# Copy to .env and fill values
# Vercel Blob read/write token (keep secret)
BLOB_READ_WRITE_TOKEN=store_N4FDh4Z6cHrTGBnV_READ_WRITE_TOKEN="vercel_blob_rw_N4FDh4Z6cHrTGBnV_JAeUvUxJ9c8PpKLm3QGs7ipplT41S6"
# Admin credentials (change before deploying)
ADMIN_USER=admin
ADMIN_PASS=password
# JWT secret (change to a long random value)
JWT_SECRET=change_this_to_a_long_random_secret
# Optional: metadata path in the blob store
METADATA_PATH=metadata/mods.json
# Example env file for MGMODS (blob-only)
# Copy this to .env and fill values
BLOB_READ_WRITE_TOKEN="your_vercel_blob_token_here"
METADATA_PATH="metadata/mods.json"
VERCEL_BLOB_STORE="store_xxx"
VERCEL_BLOB_REGION="iad1"
VERCEL_BLOB_BASE_URL="https://<store>.public.blob.vercel-storage.com"
PORT=3000
